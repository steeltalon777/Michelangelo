[package]
name = "michelangelo_workspace"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
michelangelo_protocol = { path = "../michelangelo_protocol" }

[dev-dependencies]
tempfile = "3"
